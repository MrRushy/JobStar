spring.datasource.url=jdbc:postgresql://localhost:5432/jobstar_test
spring.datasource.username=postgres
spring.datasource.password=jobstar-dev

# Integration tests start from an empty schema and remove it when the test context closes.
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
